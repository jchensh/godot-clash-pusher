# PLAN_V5.md — V5 规划：实时在线 F2P 闯关养成（服务器权威）

> 本文件是 **V5 阶段的权威规划**。V1/V2/V3 已完成；V4 联网地基 S0~S4 完成（账号/档案/lockstep/匹配），**决策 48 起转为 V5 在线主干**。全局 roadmap 见 [PLAN_GRAND.md](PLAN_GRAND.md)。
>
> ## ⚠️ 决策 48 转向（2026-06-26）：单机 → 实时在线 F2P、服务器权威
> 用户拍板**推翻决策 47「单机本地、不依赖服务端」**，项目按**多人在线网游标准**开发：①进游戏强制登录 + 持久连接、**断线即不可玩**；②**服务器唯一权威**（账号/钱包/货币/卡养成/关卡进度/挂机/配置全在服务器 + PG DB，**服务器时钟**结算）；③配置服务器化（登录下发带版本配置包，客户端只内存 + 薄缓存、非零配置——战斗 sim 仍需 units/cards/skills/arena 在客户端算）；④客户端 = **瘦表现层**（UI + 客户端 lockstep 战斗 sim + 非权威缓存）；⑤战斗仍客户端 lockstep（服务器下发权威输入，胜负先信任客户端 + sanity 限制，复算/反作弊并入后续）；⑥复用 V4 的 Go+PG+账号+WS+lockstep。
> **本文件性质**：§4~§8、§10 的**游戏设计（战力模型/养成/经济/闯关/资源/配置表）仍有效**——只是"谁算、存哪"从客户端本地改为**服务器权威**；存储/施工以 §9 + §11 的在线版为准。本地原型 S0~S6 算法**镜像进 Go 做权威**、客户端那份保留做 UI 预览 + 战斗内计算。
>
> **开发纪律沿用 V1~V4**：一步一确认、每步 commit、逻辑步必配单测（客户端 GDScript + 服务端 Go）、不过度设计、每步同步 HISTORY.md + Jira（project KAN）。表现/真机步交真人验收。
> **执行原则**：本文件是地图不是合同，数值一律〔示意·待平衡〕。

---

## 1. V5 目标一句话

**实时在线 F2P 闯关养成手游（服务器权威）**：进游戏登录 + 持久连接，**100+ 关在线闯关**（配置化难度系数）+ **货币经济**（金币/碎片/宝石）+ **卡牌养成**（升级提数值、升阶解锁技能）+ **资源获取**（推关奖励 + **服务器时钟**挂机产出）。养成/经济/进度**全服务器权威**；客户端瘦表现层、战斗客户端 lockstep。F2P，IAP 支付/合规按上线节奏（V6+）。

**核心循环（F2P 留存引擎）**：

```
推关（花操作/时间）→ 掉落金币 + 卡牌碎片 → 养成卡牌（升级=数值↑ / 升阶=数值跳变 + 解锁新技能）
   → 战力↑ → 攻克更高难度系数的关 → 循环。  挂机离线产金币，平滑养成节奏 + 给回流理由。
```

四个系统不是 4 个孤立功能，而是**一条经济链**：关卡=产出口，养成=消耗口，难度系数=闸门，挂机=平滑水龙头。

---

## 2. 已锁定决策（用户 2026-06-26 确认）

| 维度 | 决定 |
|---|---|
| **核心范式** | **战力为底·操作为顶**（hybrid）：养成给**有意义但有上限**的数值；难度来自①数值系数②AI 行为档③敌方卡组变脏。中等战力差能靠操作弥补、巨大战力差不能 → 既保住技巧战斗的乐趣，又给养成爽感。 |
| **关卡产出** | **模板池 × 难度系数曲线**：手工 ~15 个遭遇模板（敌方卡组 + AI 行为原型）+ 配置化系数递增 + 每章主题 + boss 手工特化。 |
| **养成深度** | **10 级 × 3 阶**（浅养成）：单卡满 ≈ ×3.0 hp/dmg；养成是轻量加分层，战斗更偏操作。 |
| **难度曲线** | **线性平滑递增**（系数 1.0 → ~2.6）+ boss 小跳（×1.1）。 |
| **集卡维度** | **初始 8 张（= 现默认卡组）+ 推关逐步解锁其余 8 张**（攒碎片解锁）。 |
| **货币** | **金币（软，升级）+ 卡牌碎片（每卡，解锁+升阶）+ 宝石（高级币占位，前期只产不充）**。 |
| **升级/升阶** | **升级花金币**（平滑数值，高频）；**升阶花碎片+金币**（里程碑，数值跳变 + 解锁技能积木 + 抬等级上限）。 |
| **节奏闸门** | **挂机离线金币（按进度产出、封顶）+ 无体力**（关卡随便刷，靠重复奖励边际递减自然限速）。 |
| **平台/部署（决策 48 改）** | ~~全程单机本地~~ → **实时在线、服务器权威**：强制登录 + 持久连接、断线不可玩；账号/钱包/养成/进度/配置全在服务器 + PG；客户端瘦表现层 + 非权威缓存。复用 V4 Go+PG+账号+WS+lockstep。 |

**与 V4 的关系**：V4-S0~S4 成果（账号/档案/lockstep/匹配）保留在仓库不动；V4-S5（赛季/榜，Jira KAN-41）退回 To Do 暂缓。联机对战将来若重启，养成数据需纳入 lockstep 同步（届时再设计，不在 V5 范围）。

---

## 3. 架构与现有系统复用（不重造轮子）

**决策 48 后**：经济/养成/进度/配置**服务器权威**（Go + PG），客户端瘦表现层。下表的本地件（S0~S6 已写）= **原型 + 客户端侧 UI/战斗计算**；其结算算法**镜像进 Go 做权威**。新增管线："出兵数值乘区"（已做 S1）+ **服务器经济服务 + 持久会话连接 + 配置下发**（N1~N7，见 §11）。

| 现有件 | V5 用途 |
|---|---|
| `logic/skill_system.gd` | **新增**：spawn 路径接受 unit stat multiplier（hp/dmg），是 V5-S1 地基。 |
| `logic/match.gd` | 已有 `setup(..., modifiers)`；V5 注入双方乘区（我方按卡 level/rank、敌方按关卡 coef）。 |
| `logic/run_modifiers.gd` | 已做 **level 级**数值修正（圣水/塔血）；V5 复用其叠加引擎思路，新增 **unit 级**乘区（代码注释早标"留后续"）。 |
| `logic/campaign_state.gd` | 线性可重试推进 + cleared 状态 → **闯关 StageProgress 的直接范本**。 |
| `logic/save_system.gd` | `user://` JSON 存读范式 → 扩展出 PlayerData（钱包/卡牌/关卡/挂机）。 |
| `logic/meta_progress.gd` | 解锁门控解算范式 → 卡牌解锁/章节解锁复用。 |
| `logic/config_loader.gd` | 字典输出 + 交叉引用校验 → 接入新表（stages/encounters/economy/card_progression）。 |
| `view/deck_builder*` | 接"只用已解锁卡 + 显示等级/阶"。 |
| 战斗确定性（10Hz、无随机） | 乘区只改 hp/dmg 常量、**不动 speed/range/tick**，确定性不受影响、可单测。 |

---

## 4. 战力模型（hybrid 的灵魂，全系统地基）

**机制**：出兵时单位 `hp` / `damage` 各乘一个系数；`attack_speed` / `move_speed` / `attack_range` **永不缩放**（保手感 + 确定性）。
- 我方乘区 = f(卡 level, 卡 rank)；敌方乘区 = 关卡 `difficulty_coef`。
- 敌塔 HP 随 coef 放大（或每关 `tower_hp` 显式配，二选一，S3 定）。

| 来源 | 公式〔示意·待平衡〕 | 满值 |
|---|---|---|
| **升级** Level 1→10 | 每级 hp/dmg +10%（线性叠加） | ×1.9 |
| **升阶** Rank 1→3 | 每阶 ×1.25（2 次跳变） | ×1.56 |
| **我方满养成** | 升级 × 升阶 | **≈ ×3.0** |
| **敌方难度系数** | `coef = 1.0 + (stage_global_index − 1) × 0.016` | 第 100 关 ≈ ×2.6（boss ×1.1） |

**操作可弥补窗口**（hybrid 调参核心）：
| 我方队伍战力 vs 关卡推荐 | 体感 |
|---|---|
| ≥ 推荐 | 正常局，拼操作/构筑 |
| 落后 ≤ ~15% | 兵更脆，**高手能靠走位/防守过** |
| 落后 > ~25–30% | 兵被秒，**几乎必须先养成** |

> 窗口宽窄 = (升级加成幅度) vs (系数曲线斜率) 的博弈，是 V5-S8 平衡 pass 的头号旋钮。

**worked example**：食人魔基础 2000hp。第 50 关 coef ≈ ×1.78 → 敌方食人魔 ≈ 3560hp。你练到 8 级 2 阶（×1.7×1.25 ≈ ×2.13）→ 4250hp，小优势 → 操作局；练满 ×3.0 → 6000hp → 碾压。这就是"差一级就过"的卡点钩子。

**推荐战力（power rating）**：每卡按 `base_power[rarity] × (1 + (level−1)×0.10) × rank_mult` 折算 power，队伍 = 8 卡之和；每关配 `recommended_power`；UI 显示「我的战力 X / 推荐 Y」红绿提示，告诉玩家"该养成了"。

---

## 5. 卡牌养成（稀有度 + 升阶技能解锁）

复用 **skill 积木系统**——升阶 = 给卡的 `skills` 数组**加/改积木**（真·解锁新技能，不只是数字变大）。3 阶 = 2 次阶跃（1→2、2→3），各解锁一个积木/增强。

| 卡 | 稀有 | 初始 | 2 阶解锁 | 3 阶解锁 |
|---|---|---|---|---|
| 骑士 knight | 普通 | ✅ | 数值跳 | 攻击附带小溅射(on-hit aoe) |
| 弓箭手 archers | 普通 | ✅ | 数值跳 | +1 弓手(2→3) |
| 哥布林 goblins | 普通 | ✅ | +1 只(3→4) | 数值跳 |
| 箭雨 arrows | 普通 | ✅ | 半径+ | 伤害+ |
| 电火花 zap | 普通 | ✅ | 伤害+ | 命中范围+ |
| 骷髅兵 skeletons | 普通 | 🔒 | +1 骨(4→5) | +1 骨(5→6) |
| 食人魔 giant | 稀有 | ✅ | 数值跳 | 亡语小溅击(死亡 aoe) |
| 怨灵 minions | 稀有 | ✅ | +1 只(3→4) | 数值跳 |
| 火球术 fireball | 稀有 | ✅ | 半径+ | 伤害+灼烧 |
| 狂战士 mini_pekka | 稀有 | 🔒 | 数值跳 | 对塔/建筑加伤 |
| 滚石 log | 稀有 | 🔒 | 滚更远(range+) | 伤害+ |
| 女巫 musketeer | 史诗 | 🔒 | 数值跳 | 射程+/穿透 |
| 余烬火颅 baby_dragon | 史诗 | 🔒 | 数值跳 | 溅射半径+ |
| 闪电术 lightning | 史诗 | 🔒 | 伤害+ | 连锁第二目标 |
| 治愈术 heal | 史诗 | 🔒 | 治疗量+ | 半径+/附护盾 |
| 亡灵巨像 golem | 传说 | 🔒 | 解锁亡语召唤(death_spawn) | 召唤更多/更强 |

> 初始 8 张 = 现 `level_01` 默认卡组（knight/archers/giant/goblins/minions/fireball/arrows/zap）。
> `golem` 现在写死的 `death_spawn` → 改成 **2 阶解锁**（1 阶无亡语），作为"升阶解锁技能"的示范实现。
> 稀有度配比：普通 6 / 稀有 5 / 史诗 4 / 传说 1（传说位将来加新卡时扩充）。

---

## 6. 货币经济账本（faucet / sink）

| 币 | 产出口（faucet） | 消耗口（sink） |
|---|---|---|
| **金币 Gold** | 首通/重复关卡、挂机、日常任务 | 卡牌**升级**（主水池） |
| **碎片 Shards**（每卡独立） | 指定关掉落、boss、星宝箱、商店 | **解锁**新卡 + 卡牌**升阶** |
| **宝石 Gems**（占位） | 首通、成就、日常、星宝箱 | 商店换碎片/金币（**前期只产不充**，IAP 留后） |

**成本曲线〔示意·待平衡〕**：

| 稀有 | 升满(1→10)累计金币 | R1→R2 | R2→R3 | 解锁攒碎片 | base_power |
|---|---|---|---|---|---|
| 普通 | ~4k | 20 片 + 2k | 50 片 + 5k | 30 | 100 |
| 稀有 | ~8k | 30 片 + 4k | 80 片 + 10k | 50 | 140 |
| 史诗 | ~16k | 40 片 + 8k | 120 片 + 20k | 80 | 190 |
| 传说 | ~30k | 60 片 + 15k | 200 片 + 40k | 120 | 260 |

设计意图：**金币是主循环**（高频升级），**碎片是稀缺门槛**（解锁 + 升阶卡点），**宝石是稀有润滑/未来变现位**。

---

## 7. 闯关结构（pillar ①，配置表驱动）

**章节制**：~10 章 × 10–12 关 ≈ 100–120 关，黑暗中世纪主题区：
①荒芜村庄(教学/并入新手战役) ②幽暗墓园(亡灵) ③兽人营地(快攻) ④食人魔沼泽(坦克) ⑤巫妖回廊(法术) ⑥吸血鬼城堡(均衡) ⑦龙脊山道(空军) ⑧亡者王座(综合) ⑨深渊裂隙(高系数) ⑩终末战场(boss 综合)。章末 boss 关手工特化（特殊阵容 + 抬塔血 + 高 AI 档）。

**`stages.json`（新表）每关 schema〔示意〕**：
```jsonc
"stage_2_3": {
  "chapter": 2, "index": 3,
  "encounter": "tank_push_a",        // 引用遭遇模板
  "difficulty_coef": 1.18,           // 敌方数值乘区
  "ai_difficulty": "normal",         // 复用现有 rookie→extreme 5 档
  "recommended_power": 1200,
  "stars": [{"goal":"win"}, {"goal":"king_hp_pct","min":0.5}, {"goal":"time_under","sec":90}],
  "first_clear": {"gold":500, "gems":10, "shards":{"minions":5}},
  "repeat": {"gold":60},
  "shard_drop": {"minions":{"chance":0.3,"amount":1}}
}
```

**`encounters.json`（遭遇模板池 ~15 个）**：按原型分类——坦克推进 / 快攻群海 / 空中突袭 / 法术压制 / 均衡控制 / 亡灵海 / 双坦肉盾 / 远程风筝 / 费用倾泻 / 分推骚扰……每章选模板子集 × 系数递增；boss 关手工特化。
```jsonc
"tank_push_a": { "deck": ["giant","musketeer","minions",...], "archetype": "tank" }
```

**星级**：每关 3 星（1 星=通关，2/3 星=保王塔≥50% / 限时 / 不损公主塔等）；章内星数累计 → 解锁章节宝箱（碎片 + 宝石）。

---

## 8. 资源获取（pillar ④）

- **推关**：首通=大额一次性（金币 + 碎片 + 可能解锁新卡 + 宝石）；重复=小额金币（**边际递减**，鼓励刷但不无限刷）。
- **挂机（离线产出）**：金币/hr ≈ f(最高通关章节)，离线累计**封顶**（如 8h），回游戏一键领。**纯本地时钟**（存 `last_collect_ts`），无需服务端。
- **日常任务 / 成就**：任务给金币+宝石，成就里程碑给宝石+碎片（V5-S6 占位，可后续充实）。
- **星级宝箱**：章内星数里程碑 → 碎片 + 宝石。
- **节奏闸门**：前期**无体力**，靠重复关边际递减 + 挂机封顶自然限速；体力/门票留作后续可选（偏变现，玩法验证期不上）。

---

## 9. 数据 schema（决策 48：服务器 PG 权威 + 客户端非权威缓存）

> 下面的结构是**逻辑形状**——决策 48 后落**服务器 PG 表**（per-account wallet/card_progression/stage_progress/idle_state，见 §11 N3），**不再落 `user://` 权威档**。客户端 `player_data.gd` 降级为**服务器状态的非权威本地缓存**（秒启动/只读展示，登录以服务器覆盖）。逻辑形状（原 user:// 草案，现映射为 PG 表 + 客户端缓存）：

```jsonc
{
  "wallet": { "gold": 0, "gems": 0 },
  "cards": {
    "knight": { "level": 1, "rank": 1, "shards": 0, "unlocked": true },
    "golem":  { "level": 1, "rank": 1, "shards": 0, "unlocked": false }
    // ... 16 卡
  },
  "stages": { "stage_1_1": { "stars": 3, "cleared": true } /* ... */ },
  "highest_cleared": "stage_3_5",
  "idle": { "last_collect_ts": 0 }
}
```

存档落 `logic/player_data.gd`（新建，纯逻辑、`to_dict`/`load_dict` 不引用自身 class_name，沿用 V3-4d 踩坑经验）；SaveSystem 加 save/load player data。

---

## 10. 配置表清单（新增）

| 表 | 内容 | Excel 镜像 |
|---|---|---|
| `config/stages.json` | 100+ 关定义（章节/系数/遭遇/星级/奖励/掉落） | 暂不进（结构性大表，比照 arena/run） |
| `config/encounters.json` | ~15 遭遇模板（敌方卡组 + 原型） | 暂不进 |
| `config/economy.json` | 升级/升阶/解锁成本、挂机产率、奖励基数 | **可后续进**（数值表，策划友好；平衡 pass 时定） |
| `config/card_progression.json` | 每卡稀有度 + 各阶技能积木解锁 + base_power | **可后续进** |

> 遵循"agent 默认改 JSON"；economy/card_progression 若进 Excel，走 `tools/build_config.py` 同款工作流（届时扩 sheet）。

---

## 11. 施工图细表（按序执行，每步停下确认 + commit + 单测；表现步交真人验收）

> **决策 48 后的执行顺序**：本地原型 **S0~S6 已完成**（养成/经济/闯关逻辑 + 270 单测，客户端侧）→ **在线化 N1~N7**（持久连接 + 服务器权威经济，**当前在做**）→ **S7 UI（接服务器）/ S8 内容平衡**。S0~S6 的算法镜像进 Go 做权威结算，客户端那份保留做 UI 预览 + 战斗内计算。

### 11.1 在线化施工图（N1~N7，决策 48 新增，**当前在做**）

| 步 | 类型 | 内容 | 验收标准 |
|---|---|---|---|
| **N1** | Task | 持久 WS 会话 + 登录门：客户端启动强制登录 → 建持久 WS 会话（鉴权 + 心跳 + 在线态）→ 断线"重连中"遮罩/自动退避重连/长断回登录；服务器会话管理（复用 V4 gateway/auth） | Go 单测会话生命周期；客户端 headless smoke：登录→连→心跳→断线重连 |
| **N2** | Task | 配置服务器化 + 登录下发：服务器持权威 `config/*.json`、登录后下发带版本配置包；客户端只内存持有 + 薄版本缓存（版本不符重拉） | Go 单测配置版本/下发；客户端拿配置跑战斗/UI |
| **N3** | Task | 服务器经济状态 + DB：migrations（per-account wallet/currency、card_progression、stage_progress、idle_state）+ 读取 API（会话鉴权）；客户端 `net/economy.gd` 拉取 + 缓存 | Go 单测/集成建表+读取；客户端拉档显示 |
| **N4** | Story | 服务器结算动作：升级/升阶/解锁（**服务器用服务器侧配置算成本 + 校验 + 落库**，把 `player_data` cost/curve 镜像到 Go） | Go 单测：成本/上限/不足拒；端到端 |
| **N5** | Story | 通关发奖：客户端上报 (stage_id, stars)，服务器 sanity 校验（关卡已解锁/进度连续）+ 发首通/重复奖励 + 记进度 | Go 单测：首通/重复/伪造拒；端到端 |
| **N6** | Story | 挂机服务器时钟结算：服务器存 last_collect，按**服务器时间**算产出落库（改本地时钟无效） | Go 单测：累计/封顶/改时钟无效；端到端 |
| **N7** | Task | 客户端瘦客户端化：`PlayerData` 降为服务器状态缓存；养成/领奖改调 API；开战从服务器拉权威 level/rank → 客户端确定性算战斗数值 | 客户端单测调整；**真机：改存档/改时钟均无效** |

### 11.2 本地原型施工图（S0~S8；S0~S6 已完成，S7/S8 顺延到 N7 之后）

| 步 | 类型 | 内容 | 验收标准 |
|---|---|---|---|
| **V5-S0** | Task | 配置表骨架（stages/encounters/economy/card_progression 四表 schema + 各 1–2 条样例）+ 存档 schema 设计（`player_data.gd` 草案）+ ConfigLoader 接新表 + PLAN_V5 定稿 | 配置 check 通过；ConfigLoader 读新表单测；无运行时回归（当前 221/221 基线不破） |
| **V5-S1** | Task | **出兵数值乘区管线（逻辑层地基）**：SkillSystem spawn 路径接受 unit stat 乘区（hp/dmg）；Match 注入双方乘区（我方 power_mult / 敌方 coef）；speed/range/tick 不动 | 单测：乘区缩放 hp/dmg、不动 speed/range；**乘区=1 时与现状逐位一致**（零回归） |
| **V5-S2** | Task | 玩家存档系统 `player_data.gd`（wallet/cards/stages/idle）+ SaveSystem 扩展 + 战力计算（card power → team power）+ 解锁解算 | 单测：存读往返、战力公式、解锁门控、默认新档（初始 8 张解锁） |
| **V5-S3** | Story | 闯关骨架 `stage_progress.gd`（线性章节推进，复用 CampaignState 范式）+ 星级判定（通关/塔血/限时）+ stages 配置展开 + Match 接 stage（coef + encounter + ai_difficulty） | 单测：推进/星级判定/章节解锁/存档；headless 跑通一关 |
| **V5-S4** | Story | 卡牌升级：level up（金币 sink + 数值曲线）+ 等级上限受阶限制 + 成本表 | 单测：升级扣金币、数值曲线、上限钳制、金币不足拒绝 |
| **V5-S5** | Story | 卡牌升阶：rank up（碎片 + 金币）+ **技能积木解锁**（skills 数组按阶增改）+ 抬等级上限 | 单测：升阶扣碎片、技能积木解锁正确、阶上限、碎片不足拒绝 |
| **V5-S6** | Story | 经济产出：首通/重复奖励、挂机离线金币（本地时钟 + 封顶 + 领取清零）、日常任务/成就占位、碎片掉落、解锁新卡 | 单测：奖励发放、挂机累计+封顶+领取、攒碎片解锁 |
| **V5-S7** | Story | UI 整合：闯关地图（章节/关卡/星级）+ 养成界面（升级/升阶/解锁）+ 钱包 + 挂机领取 + 战力提示 + deck builder 接已解锁卡 | **真人验收**：各屏点通、养成可视、闯关可推 |
| **V5-S8** | Story | 内容铺量：100 关 stages 配置 + ~15 遭遇模板 + 系数曲线 + **平衡 pass**（probe harness 测战力门槛与操作窗口） | **真人验收**：从第 1 章推进体验难度曲线合理；probe 测量战力/系数匹配 |

**分期取舍**：S1（乘区管线）是命门地基，先于一切养成/关卡；S2/S3 把"存档 + 闯关"立起来即可 headless 跑通；S4/S5 是养成两件套；S6 接通经济水龙头；S7 才贴 UI；S8 铺量 + 平衡（真正调手感）。S0~S6 全 headless 单测覆盖，S7/S8 交真人。

---

## 12. 待细化（到对应步骤前展开）

- **数值平衡全交 V5-S8 probe**：升级 %/级、阶乘区、系数斜率、各关 recommended_power、成本曲线、挂机产率——本文件数字均〔示意〕。
- **敌塔 HP**：随 coef 放大 vs 每关显式配（V5-S3 定）。
- **星级目标的判定口径**：保王塔/限时/不损公主塔的具体阈值（V5-S3/S8）。
- **章节主题与遭遇模板逐章排布**（V5-S8 铺量时定）。
- **技能积木解锁的逐卡精确实现**（哪些是新积木类型、哪些是改参数；on-hit aoe / 灼烧 / 连锁等若需新积木类型，在 V5-S5 评估）。
- **deck builder 是否限制按战力/费用**（V5-S7）。
- **任务/成就系统的充实度**（V5-S6 先占位，后续可单独子步）。
- **联机养成同步**：若将来重启 V4 PvP，养成数据如何进 lockstep（V5 范围外）。

> V5 锁定范围如上。支付/IAP、正式登录、云部署、联机赛季——全部不在 V5，沿用 V4 的"产品化推后"。
