# PLAN_V5_HBATTLE.md — 横版战斗施工图（表现层，🚧 H1+H2 完成待真人验收）

> 2026-07-04 用户拍板立项：**战斗加一个横向版本**——现在战场是竖的（我方在下、敌方在上，单位上下走），改成横的（我方在左、敌方在右，单位左右走）。**动机 = 帧动画素材减半**：三国热血画风的角色帧动画按"侧视图"绘制，一套侧视帧 + 水平镜像(flip_h)即可覆盖左右两个朝向；竖版则需要"背面行+正面行"两套行（现 sprite_db 的 row/row_up 结构），美术成本翻倍且侧视最出味。
> **盘面大小、地形、全部战斗逻辑不变——纯表现层改动。** 纵版保留、两版共存可切换（"加一个版本"）。
> **状态：2026-07-05 用户拍板开工「最小可看里程碑」= H1+H2+设置页实验开关，代码+单测完成（366/366），待真人验收（台账 [docs/ACCEPTANCE_SANGUO.md](docs/ACCEPTANCE_SANGUO.md) E 组）**。H3~H6 未开工。

## 0. 铁律（施工时不可违反）

1. **`logic/` 零改动**：10Hz 确定性 tick、arena tile 坐标系（18×32，y 轴=纵深）、lockstep 状态哈希、PvE 重放、AIController 全部不碰。横竖只是"同一逻辑坐标系的两种屏幕投影"。**验收硬标准：同一局重放在纵/横两版下战斗结果与逐 tick hash 完全一致**（反作弊 KAN-78/79 链路天然不受影响，因为 verifier 跑的是 logic 层）。
2. **服务端零改动**、config 数值零改动（可能新增纯表现配置项，如 settings 的版式开关，不入战斗结算）。
3. 两版共存：竖版是已真人验收的基线（KAN-49/S8e 口径都在竖版上），横版没验收通过前不得下掉竖版。

## 1. 现状盘点（写给施工 session，已核实）

- **窗口**：project.godot 720×1280 portrait，stretch=canvas_items，`handheld/orientation="portrait"`。
- **像素账（旋转完美贴合）**：场地 18 宽×32 长（arena.json grid），竖版 720/18=40px/格、1280/32=40px/格 → 横屏 1280×720 下旋转后 32→1280、18→720，**仍是 40px/格，信息密度不变**。河 y∈[15,17]、双桥 x∈[3,5]/[13,15]、部署线 player_y_min=17 / enemy_y_max=15，旋转后河变竖条、桥变上下两座。
- **坐标变换现状**：`view/battle_scene.gd` 用 `_tile_px()` 手算 tile→px 散布 **9 处**（行 264/291/324/358/504/563/605/797/856）；`view/net_battle_scene.gd` 已有**先例**——side2 视角 180° 翻转收敛在 `_t2s()` 一个映射函数 + 少数 flip 感知点（部署 y 界 383 行、精灵朝向 470 行）。➜ 横版照抄这个模式：**把变换收敛成一个函数，再在函数里加旋转分支**。
- **精灵朝向现状**：`view/sprite_db.gd` 每状态 `row`(朝下/正面) + `row_up`(朝上/背面，可选)；battle_scene 按 owner 选行。横版需要"侧视行 + flip_h"新约定；**现有素材没有侧视行**（10 个旧单位是上下行或对称帧）。
- **HUD**：手牌/圣水条/下一张/名片/血条/倒计时按 720 宽竖屏布局，横版需重排到 1280×720。
- **投射物/FX**：涟漪投射物 FX、飘字、DROP_LIFT_TILES（下落起跳偏移，竖向语义）等表现元素存在方向假设，逐个过。

## 2. 横版投影定义（施工基准）

```
竖版（现状）：sx = p.x * tile      sy = p.y * tile        （敌上我下；y 小=敌方）
横版（新）  ：sx = (grid_h - p.y) * tile    sy = p.x * tile  （敌右我左；单位向右=向敌）
net side2   ：先套横版投影，再整场左右镜像（sx' = W - sx），本方永远在左
```
- 我方在左、敌方在右（横版对推惯例，进攻方向=向右与阅读方向一致）。
- 朝向规则：单位移动/攻击目标在其右侧 → 用侧视帧原样；在左侧 → flip_h。Godot 4 用 dst Rect2 负宽或 draw_set_transform 实现镜像。
- 输入逆变换 `_s2t()` 同步旋转（点击/拖拽部署→tile），半场校验用逻辑坐标做（不受投影影响）。

## 3. 施工步骤（每步一 commit + 停下确认；H1 是无风险先手棋）

| 步 | 内容 | 验收 |
|---|---|---|
| **H1 变换层收敛（纯重构，可先行）✅ 2026-07-05** | 实盘 **14 处**方向性手算（比预估 9 处多：+塔 footprint×2/塔箭口/塔伤数锚/部署区矩形×2/教程 field 高亮）收敛进「坐标映射」区块：`_t2s/_s2t/_tile_px` 保持 + 新增 `_ur/_tile_rect/_fp_screen/_screen_up_tiles/_deploy_zone_rect` 五个语义 API，全部代数恒等替换，**竖版行为逐像素不变**（test_hbattle_transform 竖版基准锁定） | 现有单测零回归 ✅（366/366）+ 真人抽查竖版无视觉变化 ⬜（E-1） |
| **H2 横版投影 + 场景绘制 ✅ 2026-07-05** | 变换层 8 函数加 `_landscape` 分支（§2 公式；**全部改动收在坐标映射区块内**）；地形/塔/部署高亮/FX/飘字/投射物自动跟随（屏幕语义元素——影子/上浮/血条/伤数/箭矢方向——天然两版通用，零改动）。**临时投影区**：H5 切横屏窗口前，竖屏场区内按 32:18 满宽 letterbox 垂直居中（720×405，22.5px/格）。**开关**：设置页「战斗版式」竖版/横版两按钮（`GameState.battle_layout()` 存 user://settings.cfg [battle]，纯表现偏好不违决策 48；文案硬编码中文对齐 GM 区先例，H5 正式化再进 i18n）。**门控**：战役/新手引导强制竖版（教程高亮竖版语义）；net_battle 不接（H6） | 横版下打完整一局 ⬜（E-2/E-3）；重放一致=logic 零改动+闯关局 KAN-79 服务器重放校验天然背书 |
| **H3 精灵侧视约定 + 兼容期策略** | sprite_db 增加 `row_side`(+flip 规则)；**三国新素材直接按"侧视帧"约定交付**（联动美术表 A3 补约定列）；旧上下行素材兼容期＝继续用现有行不翻转（丑但可玩，等素材替换） | 帧动画在横版朝向正确（向右原样/向左镜像） |
| **H4 横版 HUD 重排** | 1280×720 布局：手牌/圣水/下一张/名片/倒计时（拇指区原则：手牌下中、两侧留部署操作区）；**mockup 先行**（遵守 UI/UX 准则记忆） | mockup 用户过目 → 实装真人验收 |
| **H5 版式切换 + 屏幕方向** | 设置项（settings.cfg）或战斗入口参数选 纵/横；进横版战斗切横屏（移动端 `DisplayServer.screen_set_orientation`，桌面调窗口/letterbox），退出恢复竖屏；主流程（菜单/养成）恒竖屏 | 切换往返无残留（分辨率/方向/HUD 复位） |
| **H6 net_battle 横版 + side2** | 横版投影 × side2 左右镜像复合；名片/胜负/落点全对 | **两台真机对战验收**（side2 视角单列用例，KAN-49 经验） |

## 4. 风险清单

- **双版本维护**：所有新表现特性从此要在两版下各验一遍 → 用"单一变换层"压成本；长期若横版胜出，竖版按用户决策再议下线。
- **side2 复合变换**（旋转×镜像）是最易错点——KAN-49 教训：side2 单独出验收用例（朝向不反/落点合法/本方数据正确）。
- 旧素材在横版的兼容期观感差（上下行贴图硬摆），属预期，素材到位即消。
- 桌面 F5 调试窗口方向切换体验（720×1280↔1280×720）需要顺手不烦人。
- 移动端刘海/安全区在横屏下的 HUD 避让（打包前必检项追加）。

## 5. Jira 建议（手工，挂 Epic KAN-50）

- 新建 Story「横版战斗（表现层）：H1 变换层收敛 ~ H6 net 横版真机验收」→ **Idea**（立项未排期；开工时转 To Do 并可按 H1~H6 折子步进描述）。

## 6. 排期建议（用户拍板）

横版的收益全部落在"三国侧视帧素材"上 → **建议排在 A3（美术清单，把"侧视帧"写进约定）之后、首批三国单位素材接入（A4）之前或同期**：素材直接按侧视做一步到位，避免美术先画上下行、横版上线后返工重画。H1（纯重构零风险）可随时先行插队。
