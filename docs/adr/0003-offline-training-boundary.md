# ADR-0003：生产主流程不离线降级

- 状态：Accepted（E0，2026-07-11）

## 决策

Prod 在登录失败或掉线时只允许登录、重连、更新提示和当前页面只读展示，不自动进入可写本地模式。若未来保留 `offline_training`，它必须是显式非权威沙盒，结果不能写回在线进度。

## 原因

自动离线降级会使玩家无法区分权威状态与本地状态，也会引入补写、冲突合并、改档和改时钟攻击面，违背决策 48。

## 验证

断网、401、配置失败和 Gateway 排空测试中，客户端不得产生可在恢复后兑换为在线资产的结果。
