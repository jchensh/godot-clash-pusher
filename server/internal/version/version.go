// Package version exposes server version constants used across cmd binaries.
package version

const (
	// V4Stage tracks which V4 sub-step the binary corresponds to.
	V4Stage = "V4-S4"

	// Build is a short build label appended to logs.
	Build = "scaffold"
)
